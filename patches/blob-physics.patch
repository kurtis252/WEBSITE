diff --git a/index.html b/index.html
index 38e4ac6..3a8ce43 100644
--- a/index.html
+++ b/index.html
@@ -418,6 +418,7 @@ class Component extends DCLogic {
         depth: parseFloat(el.dataset.depth || "1"),
         par: parseFloat(el.dataset.par || "0"),
         inner: el.querySelector("[data-squish]"),
+        front: !!(el.closest && el.closest("[data-front]")),
         cx: 0, cy: 0, x: 0, y: 0, dvx: 0, dvy: 0,
         ph: i * 1.83,
         sp: 0.000072 + (i % 5) * 0.000011,
@@ -448,6 +449,7 @@ class Component extends DCLogic {
     window.addEventListener("touchmove", this.onMove, { passive: true });
 
     this.setupTilt(root);
+    this.setupDrag(root);
     this.reveal(root);
 
     if (!this.off && !window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
@@ -463,7 +465,8 @@ class Component extends DCLogic {
     window.removeEventListener("pointermove", this.onMove);
     window.removeEventListener("touchstart", this.onMove);
     window.removeEventListener("touchmove", this.onMove);
-    if (this.onTilt) window.removeEventListener("deviceorientation", this.onTilt);
+    if (this.detachTilt) this.detachTilt();
+    if (this.detachDrag) this.detachDrag();
     if (this.io) this.io.disconnect();
     if (this.cio) this.cio.disconnect();
   }
@@ -552,8 +555,40 @@ class Component extends DCLogic {
   }
 
   setupTilt(root) {
-    if (!("DeviceOrientationEvent" in window)) return;
+    // No "enable motion" button: the pill is gone and permission is asked for
+    // silently, off the back of whatever the visitor does first.
     const ask = root.querySelector("[data-tilt-ask]");
+    if (ask && ask.parentNode) ask.parentNode.removeChild(ask);
+
+    const touch = "ontouchstart" in window || navigator.maxTouchPoints > 0;
+
+    // Add ?tiltdebug=1 to the URL for a readout of what the device actually
+    // reports. Invisible to everyone else.
+    const diag = /[?&]tiltdebug=1/.test(location.search) ? { rel: 0, abs: 0, perm: "not asked", last: "no events yet", err: "", tries: 0 } : null;
+    let panel = null;
+    if (diag) {
+      panel = document.createElement("div");
+      panel.style.cssText = "position:fixed;left:8px;bottom:8px;z-index:99999;font:11px/1.5 ui-monospace,Menlo,Consolas,monospace;background:rgba(0,0,0,.85);color:#79ffb0;padding:10px 12px;border-radius:9px;max-width:80vw;white-space:pre;pointer-events:none";
+      document.body.appendChild(panel);
+    }
+    const paint = () => {
+      if (!panel) return;
+      panel.textContent =
+        "DeviceOrientationEvent: " + ("DeviceOrientationEvent" in window) +
+        "\ngated(requestPermission): " + (typeof (window.DeviceOrientationEvent || {}).requestPermission === "function") +
+        "\npermission: " + diag.perm + "  (asks: " + diag.tries + ")" +
+        "\nsecure: " + window.isSecureContext + "   touch: " + touch +
+        "\nevents  deviceorientation: " + diag.rel +
+        "\n        ...absolute: " + diag.abs +
+        "\ntilt active: " + this.t.has +
+        "\n" + diag.last + (diag.err ? "\nerr: " + diag.err : "");
+    };
+    paint();
+
+    if (!("DeviceOrientationEvent" in window)) return;
+    // Tilt is a phone/tablet idea. Desktop keeps the cursor parallax.
+    if (!touch) return;
+
     this.onTilt = (e) => {
       if (e.gamma == null && e.beta == null) return;
       this.t.g = e.gamma || 0;
@@ -562,26 +597,163 @@ class Component extends DCLogic {
         this.t.has = true;
         this.t.nx = 0;
         this.t.ny = 0;
-        if (ask) ask.style.display = "none";
       }
     };
-    // iOS 13+ gates motion sensors behind an explicit user gesture.
-    const gated = typeof DeviceOrientationEvent.requestPermission === "function";
-    if (!gated) {
-      window.addEventListener("deviceorientation", this.onTilt, { passive: true });
+
+    const n = (v) => (v == null ? "null" : v.toFixed(1));
+    const tap = (kind) => (e) => {
+      if (diag) {
+        diag[kind]++;
+        diag.last = kind + "  a:" + n(e.alpha) + " b:" + n(e.beta) + " g:" + n(e.gamma);
+      }
+      this.onTilt(e);
+      if (diag) paint();
+    };
+    const relHandler = tap("rel"), absHandler = tap("abs");
+
+    let attached = false;
+    const attach = () => {
+      if (attached) return;
+      attached = true;
+      window.addEventListener("deviceorientation", relHandler, { passive: true });
+      // Plenty of Android devices have no relative-orientation sensor and fire
+      // only the absolute event — listening to just "deviceorientation" there
+      // gets you either nothing at all or null gamma/beta.
+      window.addEventListener("deviceorientationabsolute", absHandler, { passive: true });
+    };
+    this.detachTilt = () => {
+      window.removeEventListener("deviceorientation", relHandler);
+      window.removeEventListener("deviceorientationabsolute", absHandler);
+    };
+
+    if (typeof DeviceOrientationEvent.requestPermission !== "function") {
+      attach();
       return;
     }
-    const touch = "ontouchstart" in window || navigator.maxTouchPoints > 0;
-    if (!ask || !this.small || !touch) return;
-    ask.style.display = "inline-flex";
-    ask.addEventListener("click", () => {
-      DeviceOrientationEvent.requestPermission().then((state) => {
-        if (state === "granted") {
-          window.addEventListener("deviceorientation", this.onTilt, { passive: true });
-        }
-        ask.style.display = "none";
-      }).catch(function () { ask.style.display = "none"; });
-    });
+
+    // Where the API is gated, attach anyway: on Chrome the events often flow
+    // regardless, and a listener that never fires costs nothing.
+    attach();
+    const request = () => {
+      if (diag) { diag.tries++; paint(); }
+      try {
+        const p = DeviceOrientationEvent.requestPermission();
+        if (p && p.then) p.then(function (state) {
+          if (diag) { diag.perm = state; paint(); }
+          if (state === "granted") attach();
+        }).catch(function (e) {
+          if (diag) { diag.perm = "rejected"; diag.err = String(e && e.message || e); paint(); }
+        });
+      } catch (err) {
+        // iOS throws unless this came from a user gesture — the handlers below retry.
+        if (diag) { diag.perm = "threw (needs gesture)"; diag.err = String(err && err.message || err); paint(); }
+      }
+    };
+    request();
+    const onGesture = () => { request(); };
+    window.addEventListener("touchend", onGesture, { once: true, passive: true });
+    window.addEventListener("pointerdown", onGesture, { once: true, passive: true });
+  }
+
+  // Shove a blob aside with a swipe, so it can be cleared off anything it covers.
+  setupDrag(root) {
+    // Throwing and docking is a phone idea; a mouse keeps the lighter shove.
+    this.touchDevice = "ontouchstart" in window || navigator.maxTouchPoints > 0;
+    const pick = (x, y) => {
+      let best = null, bestD = Infinity;
+      for (let i = 0; i < this.blobs.length; i++) {
+        const b = this.blobs[i];
+        if (b.el.style.display === "none") continue;
+        const r = b.el.getBoundingClientRect();
+        if (!r.width || r.bottom < 0 || r.top > window.innerHeight) continue;
+        const cx = r.left + r.width / 2, cy = r.top + r.height / 2;
+        const d = Math.hypot(x - cx, y - cy);
+        if (d < Math.max(r.width, r.height) / 2 && d < bestD) { bestD = d; best = b; }
+      }
+      return best;
+    };
+
+    let drag = null, sx = 0, sy = 0, lx = 0, ly = 0, lt = 0, decided = false, pen = false, hold = 0;
+
+    const engage = () => {
+      if (!drag || decided) return;
+      decided = true;
+      drag.held = true;
+      drag.parked = false;
+      drag.ovx = 0;
+      drag.ovy = 0;
+    };
+
+    const down = (e) => {
+      if (e.pointerType === "mouse" && e.button !== 0) return;
+      if (e.target && e.target.closest && e.target.closest("a,button,input,textarea,select,label,iframe,image-slot,form")) return;
+      const b = pick(e.clientX, e.clientY);
+      if (!b) return;
+      drag = b;
+      pen = e.pointerType !== "mouse";
+      sx = lx = e.clientX; sy = ly = e.clientY;
+      lt = performance.now();
+      b.grabX = b.ox || 0;
+      b.grabY = b.oy || 0;
+      decided = false;
+      clearTimeout(hold);
+      if (pen) {
+        // Hold still for a moment and the blob comes up in your hand, after
+        // which it can be thrown any direction. A plain swipe still scrolls.
+        hold = setTimeout(engage, 220);
+      } else {
+        engage();
+      }
+    };
+
+    const move = (e) => {
+      if (!drag) return;
+      const dx = e.clientX - sx, dy = e.clientY - sy;
+      if (!decided) {
+        if (Math.hypot(dx, dy) < 10) return;
+        clearTimeout(hold);
+        // Before the hold completes, a sideways drag still grabs the blob;
+        // an up-or-down drag is the page being scrolled, so let it go.
+        if (Math.abs(dx) < Math.abs(dy) * 0.8) { drag = null; return; }
+        engage();
+      }
+      drag.ox = drag.grabX + dx;
+      drag.oy = drag.grabY + dy;
+      const now = performance.now(), dt = Math.max(8, now - lt);
+      // Cap the throw so a hard flick can't fire a blob clean off the page.
+      drag.ovx = Math.max(-42, Math.min(42, (e.clientX - lx) / dt * 16));
+      drag.ovy = Math.max(-42, Math.min(42, (e.clientY - ly) / dt * 16));
+      lx = e.clientX; ly = e.clientY; lt = now;
+    };
+
+    const up = () => {
+      clearTimeout(hold);
+      if (drag && decided) {
+        drag.held = false;
+        drag.thrown = this.touchDevice;   // desktop keeps the old drift-home feel
+      } else if (drag) {
+        drag.held = false;
+      }
+      drag = null;
+      decided = false;
+    };
+
+    // While a blob is genuinely in hand, the page must not scroll under it.
+    const block = (e) => { if (drag && decided) e.preventDefault(); };
+
+    root.addEventListener("pointerdown", down, { passive: true });
+    window.addEventListener("pointermove", move, { passive: true });
+    window.addEventListener("pointerup", up, { passive: true });
+    window.addEventListener("pointercancel", up, { passive: true });
+    window.addEventListener("touchmove", block, { passive: false });
+    this.detachDrag = () => {
+      clearTimeout(hold);
+      root.removeEventListener("pointerdown", down);
+      window.removeEventListener("pointermove", move);
+      window.removeEventListener("pointerup", up);
+      window.removeEventListener("pointercancel", up);
+      window.removeEventListener("touchmove", block);
+    };
   }
 
   reveal(root) {
@@ -622,6 +794,9 @@ class Component extends DCLogic {
     const px = m.x, py = m.y + sy;
 
     const t = this.t;
+    // Tilt drives the front layer only; everything behind keeps the cursor and
+    // scroll parallax it already had.
+    let tnx = nx, tny = ny;
     if (t.has) {
       const ang = (window.screen && window.screen.orientation && window.screen.orientation.angle) || 0;
       let gx = t.g, gy = t.b - 42;
@@ -630,8 +805,8 @@ class Component extends DCLogic {
       const cl = function (v) { return Math.max(-1, Math.min(1, v / 24)); };
       t.nx += (cl(gx) * 0.9 - t.nx) * 0.06;
       t.ny += (cl(gy) * 0.9 - t.ny) * 0.06;
-      nx = t.nx;
-      ny = t.ny;
+      tnx = t.nx;
+      tny = t.ny;
     }
 
     const now = performance.now();
@@ -641,6 +816,65 @@ class Component extends DCLogic {
       const fx = Math.sin(now * b.sp + b.ph) * b.amp;
       const fy = Math.cos(now * b.sp * 0.81 + b.ph * 1.6) * b.amp * 0.78;
 
+      // Shove offset: carries the throw's momentum, then eases home over a few
+      // seconds so a swiped-aside blob clears the way without stranding itself.
+      if (b.thrown && !b.held) {
+        // Thrown on a phone: carry the momentum, bounce off the edges of the
+        // screen, then dock against whichever side it ends up nearest so it
+        // parks out of the way rather than sitting over the content.
+        b.ox = (b.ox || 0) + (b.ovx || 0);
+        b.oy = (b.oy || 0) + (b.ovy || 0);
+        b.ovx = (b.ovx || 0) * 0.978;
+        b.ovy = (b.ovy || 0) * 0.978;
+
+        const r = b.el.getBoundingClientRect();
+        const rad = Math.max(r.width, r.height) / 2;
+        const bcx = r.left + r.width / 2, bcy = r.top + r.height / 2;
+        const restitution = 0.62;
+        // Horizontally a blob may sit half off the screen: that is what gets it
+        // clear of the text. Vertically it stays mostly on, so it never just
+        // disappears off the top or bottom.
+        const minX = 0, maxX = vw;
+        const minY = rad * 0.4, maxY = vh - rad * 0.4;
+        if (bcx < minX && b.ovx < 0) { b.ovx = -b.ovx * restitution; b.ox += minX - bcx; }
+        else if (bcx > maxX && b.ovx > 0) { b.ovx = -b.ovx * restitution; b.ox -= bcx - maxX; }
+        if (bcy < minY && b.ovy < 0) { b.ovy = -b.ovy * restitution; b.oy += minY - bcy; }
+        else if (bcy > maxY && b.ovy > 0) { b.ovy = -b.ovy * restitution; b.oy -= bcy - maxY; }
+
+        if (!b.parked && Math.hypot(b.ovx, b.ovy) < 0.7) {
+          // Slow enough to settle: ease toward the nearer side. Gently — this
+          // is a suggestion, not a magnet.
+          const targetX = bcx < vw / 2 ? minX : maxX;
+          const dxs = targetX - bcx;
+          b.ox += dxs * 0.035;
+          b.ovx *= 0.88;
+          b.ovy *= 0.88;
+          // Once it is roughly there, let go entirely. Holding it to the pixel
+          // is what made it feel stuck; from here the blob just drifts and
+          // breathes like the others, and tilt can still push it around.
+          if (Math.abs(dxs) < 14 && Math.hypot(b.ovx, b.ovy) < 0.12) {
+            b.parked = true;
+            b.ovx = 0;
+            b.ovy = 0;
+          }
+        }
+      } else if (!b.held && (b.ox || b.oy || b.ovx || b.ovy)) {
+        // Desktop: a shove drifts back to where it belongs.
+        const cap = Math.min(300, vw * 0.45);
+        b.ox = (b.ox || 0) + (b.ovx || 0);
+        b.oy = (b.oy || 0) + (b.ovy || 0);
+        b.ovx = (b.ovx || 0) * 0.90;
+        b.ovy = (b.ovy || 0) * 0.90;
+        b.ox -= b.ox * 0.018;
+        b.oy -= b.oy * 0.018;
+        if (b.ox > cap) { b.ox = cap; b.ovx *= 0.4; } else if (b.ox < -cap) { b.ox = -cap; b.ovx *= 0.4; }
+        if (b.oy > cap) { b.oy = cap; b.ovy *= 0.4; } else if (b.oy < -cap) { b.oy = -cap; b.ovy *= 0.4; }
+        if (Math.abs(b.ox) < 0.3 && Math.abs(b.oy) < 0.3 && Math.abs(b.ovx) < 0.05 && Math.abs(b.ovy) < 0.05) {
+          b.ox = 0; b.oy = 0; b.ovx = 0; b.ovy = 0;
+        }
+      }
+      const ox = b.ox || 0, oy = b.oy || 0;
+
       if (b.mode === "repel") {
         const dx = b.vx + b.x - m.x;
         const dy = b.vy + b.y - m.y;
@@ -651,9 +885,13 @@ class Component extends DCLogic {
           b.dvx += (dx / d) * imp;
           b.dvy += (dy / d) * imp;
         }
-        if (t.has) {
-          b.dvx += nx * 0.62 * g;
-          b.dvy += ny * 0.62 * g;
+        if (t.has && b.front) {
+          // Leaning the phone is a left-and-right idea. Feeding the same force
+          // down the y axis turns them into dead weight that pools at the
+          // bottom of the screen, so vertical gets barely a nudge and is held
+          // on station by a much firmer spring below.
+          b.dvx += tnx * 0.34 * g;
+          b.dvy += tny * 0.05 * g;
         } else if (!m.has || now - (m.last || 0) > 1800) {
           b.wt = (b.wt || 0) + 1;
           if (b.wt > 100 + (i % 6) * 26) {
@@ -662,9 +900,15 @@ class Component extends DCLogic {
             b.dvy += (Math.random() - 0.5) * 1.7 * g;
           }
         }
-        const k = (t.has ? 0.0004 : this.small ? 0.0024 : 0.0011) * g;
-        b.dvx += -b.x * k;
-        b.dvy += -b.y * k;
+        // While tilting, the two axes want different rules: sideways is free to
+        // roam so a lean actually carries the blob, vertical is sprung hard so
+        // it keeps floating at its own height instead of sinking.
+        const tilting = t.has && b.front;
+        const base = this.small ? 0.0024 : 0.0011;
+        const kx = (tilting ? 0.00225 : base) * g;
+        const ky = (tilting ? 0.009 : base) * g;
+        b.dvx += -b.x * kx;
+        b.dvy += -b.y * ky;
         b.dvx *= 0.974;
         b.dvy *= 0.974;
         b.x += b.dvx;
@@ -673,7 +917,7 @@ class Component extends DCLogic {
         if (b.x > b.maxX) { b.x = b.maxX; b.dvx = -Math.abs(b.dvx) * 0.5; }
         if (b.y < b.minY) { b.y = b.minY; b.dvy = Math.abs(b.dvy) * 0.5; }
         if (b.y > b.maxY) { b.y = b.maxY; b.dvy = -Math.abs(b.dvy) * 0.5; }
-        b.el.style.transform = "translate3d(" + (b.x + fx).toFixed(2) + "px," + (b.y + fy).toFixed(2) + "px,0)";
+        b.el.style.transform = "translate3d(" + (b.x + fx + ox).toFixed(2) + "px," + (b.y + fy + oy).toFixed(2) + "px,0)";
         if (b.inner) {
           const spd = Math.hypot(b.dvx, b.dvy);
           const st = Math.min(0.28, spd * 0.03) * g;
@@ -685,11 +929,12 @@ class Component extends DCLogic {
           }
         }
       } else {
-        const tx = nx * 150 * b.depth * g;
-        const ty = ny * 110 * b.depth * g - (sy - b.cy + vh * 0.5) * b.par * 0.09 * g;
+        const unx = b.front ? tnx : nx, uny = b.front ? tny : ny;
+        const tx = unx * 150 * b.depth * g;
+        const ty = uny * 110 * b.depth * g - (sy - b.cy + vh * 0.5) * b.par * 0.09 * g;
         b.x += (tx - b.x) * 0.08;
         b.y += (ty - b.y) * 0.08;
-        b.el.style.transform = "translate3d(" + (b.x + fx).toFixed(2) + "px," + (b.y + fy).toFixed(2) + "px,0) rotate(" + (fx * 0.07).toFixed(2) + "deg)";
+        b.el.style.transform = "translate3d(" + (b.x + fx + ox).toFixed(2) + "px," + (b.y + fy + oy).toFixed(2) + "px,0) rotate(" + (fx * 0.07).toFixed(2) + "deg)";
       }
     }
 
